; File uploads first pass through PHP before Laravel/Livewire can validate them.
; Keep these limits above the 25 MB application limit so Laravel can return a
; useful validation message instead of PHP silently discarding the request.
upload_max_filesize = 26M
post_max_size = 32M
max_file_uploads = 20
